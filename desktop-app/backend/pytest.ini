[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --tb=short --strict-markers --cov=video_review_backend --cov-report=term-missing --cov-report=html:coverage_html
markers =
    slow: marks tests that are slow (deselect with '-m "not slow"')
