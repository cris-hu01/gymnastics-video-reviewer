name: CI

on:
  push:
    branches:
      - main
      - 'codex/**'
      - 'feature/**'
      - 'fix/**'
  pull_request:
    branches:
      - main

jobs:
  frontend-and-backend-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: desktop-app/gymclip-reviewer/package-lock.json

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install frontend dependencies
        working-directory: desktop-app/gymclip-reviewer
        run: npm ci

      - name: Frontend type check
        working-directory: desktop-app/gymclip-reviewer
        run: npm run lint

      - name: Frontend build
        working-directory: desktop-app/gymclip-reviewer
        run: npm run build

      - name: Backend syntax check
        run: |
          python -m py_compile \
            desktop-app/backend/main.py \
            desktop-app/backend/video_review_backend/*.py \
            desktop-app/backend/scripts/*.py
